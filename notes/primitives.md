#   Primitives for Ray Tracing Shapes -- Computer Graphics Day 8 -- 2/3/2025

## Tracking Entry
- dot product with direction determines if the ray is entering or exiting
- if max enter < min exit, we have hit the cube

##  Inside Objects

### How to tell if you're inside
- dot product
- invert the normal if inside the object